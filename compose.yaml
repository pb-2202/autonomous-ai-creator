services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: autonomous_creator
      POSTGRES_USER: autonomous_creator
      POSTGRES_PASSWORD: autonomous_creator
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
